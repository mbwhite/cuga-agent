# LLM provider – use one of the sections below

# Groq (default)
GROQ_API_KEY="gsk-your-groq-api-key"
MODEL_NAME="openai/gpt-oss-120b"
AGENT_SETTING_CONFIG="settings.groq.toml"

# OpenAI
# OPENAI_API_KEY="sk-your-openai-api-key"
# MODEL_NAME="gpt-4o"
# AGENT_SETTING_CONFIG="settings.openai.toml"

# OpenAI with custom base URL (LiteLLM, Azure, etc.)
# OPENAI_API_KEY="your-api-key"
# OPENAI_BASE_URL="https://api.example.com/v1"
# MODEL_NAME="gpt-4o"
# AGENT_SETTING_CONFIG="settings.openai.toml"

# MiniMax
# MINIMAX_API_KEY="your-minimax-api-key"
# MINIMAX_BASE_URL="https://api.minimax.io/v1"   # optional, override default
# MODEL_NAME="MiniMax-M3"
# AGENT_SETTING_CONFIG="settings.minimax.toml"

# WatsonX
# WATSONX_APIKEY="your-watsonx-api-key"
# WATSONX_PROJECT_ID="your-project-id"
# WATSONX_SPACE_ID="your-space-id"   # Alternative to WATSONX_PROJECT_ID
# WATSONX_URL="https://us-south.ml.cloud.ibm.com"
# AGENT_SETTING_CONFIG="settings.watsonx.toml"
# MODEL_NAME="meta-llama/llama-4-maverick-17b-128e-instruct-fp8"

# watsonx Orchestrate (wxO) — routes LLM calls through your Orchestrate tenant for
# tracing, guardrails, and entitlement-based billing. Requires: pip install "cuga[wxo]"
# (Python >= 3.11). Get the key and URL from Orchestrate: Settings -> API details ->
# Generate API key, and the Service instance URL shown there.
# WXO_API_KEY="your-wxo-api-key"
# WXO_INSTANCE_URL="https://api.dl.watson-orchestrate.ibm.com/instances/your-instance-id"
# AGENT_SETTING_CONFIG="settings.wxo.toml"
# MODEL_NAME="watsonx/openai/gpt-oss-120b"
# To target a local ADK dev server instead, omit WXO_API_KEY and set:
# WXO_INSTANCE_URL="http://localhost:4321"

# Optional: Langfuse tracing
# LANGFUSE_SECRET_KEY="sk-lf-xxx"
# LANGFUSE_PUBLIC_KEY="pk-lf-xxx"
# LANGFUSE_HOST="https://us.cloud.langfuse.com"
# DYNACONF_ADVANCED_FEATURES__LANGFUSE_TRACING=true

# Optional: OIDC auth (e.g. IBM Verify). Set auth.enabled=true in settings.toml when using.
# OIDC_CLIENT_ID="your-client-id"
# OIDC_CLIENT_SECRET="your-client-secret"
# OIDC_DISCOVERY_URL="https://your-tenant/.well-known/openid-configuration"
# OIDC_REDIRECT_URI="https://localhost:7860/manage"
# DYNACONF_AUTH__ENABLED=true
# HTTPS for local testing (required for IBM Verify redirect URIs)
# SSL_KEYFILE="deployment/certs/localhost.key"
# SSL_CERTFILE="deployment/certs/localhost.crt"

# Optional: OpenLit observability (OpenTelemetry / OTLP)
# Enable in settings.toml: [observability] openlit = true
# Install: pip install cuga[observability]
# Local testing stack: cd deployment/docker-compose/openlit && docker compose up -d
# OTEL_EXPORTER_OTLP_ENDPOINT="http://localhost:4318"
# Optional: Override protocol (default: http/protobuf, alternatives: grpc, http/json)
# OTEL_EXPORTER_OTLP_PROTOCOL="http/protobuf"

# Optional: E2B sandbox
# E2B_API_KEY="e2b_xxx"

# Optional: LLM HTTP timeout (seconds) for slow/reasoning models (OpenAI, Azure, OpenRouter only)
# CUGA_LLM_HTTP_TIMEOUT=120

# ─── EMBEDDING MODEL — Knowledge retrieval ───────────────────────
# Pick a provider for document embedding. The Knowledge tab's
# "Detected in your environment" panel auto-discovers configured
# providers and offers one-click apply.
#
# Local options (no keys needed):
#   - Fastembed (BAAI/bge-small-en-v1.5) — built-in default
#   - Ollama (nomic-embed-text) — needs `ollama serve` running
#
# Cloud / enterprise providers (alphabetical) — set the vars for the
# one(s) you use. Each block shows the canonical env var name that
# LiteLLM reads + the recommended default model.

# Azure OpenAI
# AZURE_API_KEY="your-azure-api-key"
# AZURE_API_BASE="https://your-resource.openai.azure.com/"
# AZURE_API_VERSION="2024-02-01"      # optional
# default model: azure/text-embedding-3-small

# Cohere
# COHERE_API_KEY="your-cohere-api-key"
# default model: cohere/embed-english-v3.0

# Google Gemini
# GEMINI_API_KEY="your-gemini-api-key"
# default model: gemini/text-embedding-004

# IBM Watsonx
# WATSONX_APIKEY="your-watsonx-api-key"
# WATSONX_PROJECT_ID="your-project-id"
# WATSONX_URL="https://us-south.ml.cloud.ibm.com"
# default model: watsonx/intfloat/multilingual-e5-large

# Jina AI
# JINA_AI_API_KEY="your-jina-api-key"
# default model: jina_ai/jina-embeddings-v3

# Mistral AI
# MISTRAL_API_KEY="your-mistral-api-key"
# default model: mistral/mistral-embed

# OpenAI
# OPENAI_API_KEY="sk-your-openai-api-key"
# OPENAI_BASE_URL="https://api.openai.com/v1"   # optional, override default
# default model: text-embedding-3-small

# OpenRouter
# OPENROUTER_API_KEY="sk-or-your-openrouter-api-key"
# default model: openai/text-embedding-3-small

# Together AI
# TOGETHERAI_API_KEY="your-together-api-key"
# default model: together_ai/BAAI/bge-large-en-v1.5

# Voyage AI
# VOYAGE_API_KEY="your-voyage-api-key"
# default model: voyage/voyage-3

# Knowledge engine config: see src/cuga/configurations/knowledge/knowledge_settings.toml

# Sandbox execution timeout, in seconds. settings.toml ships 30, which is right for most work.
# Raise it for agents that call slow external APIs — the events demos hit arXiv at ~5.5s per call
# plus retries and need ~120. This is deployment config, deliberately NOT something `cuga start`
# changes behind your back: a timeout that silently quadruples for everyone is a surprise.
# DYNACONF_ADVANCED_FEATURES__SANDBOX_EXECUTION_TIMEOUT=120

# ── Machine seam: POST /run and GET /run/agents ───────────────────────────────
# /run EXECUTES an agent, so it is authenticated with a shared secret. It is not covered by your
# login: /stream is gated by require_chat_access, /run is a machine endpoint and is gated by this
# token instead. With neither variable set, /run answers 401 rather than running anything.
#
# Generate one:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# The eventing service sends it as the X-Gateway-Token header on every call into CUGA, so ONE value
# configured on both processes is all a split deployment needs. CUGA_RUN_TOKEN wins if both are set.
# GATEWAY_TOKEN="paste-a-generated-secret-here"
# CUGA_RUN_TOKEN="set only if /run should use a different secret from the events layer"
#
# LOCAL DEVELOPMENT ONLY — run without a token. Warned about loudly at startup. Never set this on a
# host anyone else can reach: it makes agent execution available to whoever can open the port.
# CUGA_RUN_ALLOW_UNAUTHENTICATED=1

# ── Events-layer secrets: EVERY GATE FAILS CLOSED ─────────────────────────────
# These used to be "enforce IF configured", which meant an unset secret silently disabled the
# check — the service warned in a log and served the request anyway. They now REFUSE instead, so a
# missing secret is a 401, not an open door. Running open has to be stated out loud (below).
#
# Guards POST /api/events/hook/<name>, which RUNS AN AGENT. Unset, that endpoint refuses every
# request. Callers pass it as ?key=<value>. Generate the same way as GATEWAY_TOKEN:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# Note a URL carrying ?key= lands in browser history and proxy logs — rotate it if you ever share one.
# EVENTS_WEBHOOK_KEY="paste-a-generated-secret-here"
#
# Encrypts OAuth app client secrets entered through the Studio (Admin → OAuth apps) at rest.
# MUST BE A FERNET KEY — a DIFFERENT generator from the two above:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Using token_urlsafe here is rejected by Fernet and the secrets are then written in PLAINTEXT.
# That used to be silent; it now logs an error naming this command. Unset = plaintext (dev parity).
# ROTATION: this key decrypts stored data, and there is no re-encrypt path. What survives a change
# depends entirely on whether the secret has an env var behind it — and there ARE encrypted rows
# from the first boot, not just once someone uses the Studio:
#   backend/secrets/seed.py copies every env var matching KEY|SECRET|TOKEN|PASSWORD (minus the
#   CUGA_* prefix) into the DB `secrets` table, encrypted. GATEWAY_TOKEN, the channel tokens and
#   the LLM keys are all in there after one startup.
#   • env-backed rows SELF-HEAL. A wrong key makes get_secret() return None, the seeder reads that
#     as "absent" and re-seeds from the environment on the next boot.
#   • Studio-entered rows (OAuth app credentials) have no env source, so they are GONE — silently,
#     because the failed decrypt is a debug log and a None, not an error.
# CUGA_SECRET_KEY="paste-a-generated-fernet-key-here"
#
# LOCAL DEVELOPMENT ONLY — opens /invoke, the poll seams, the inbound webhook and Slack signature
# verification all at once, so you need none of the secrets above. The events counterpart of
# CUGA_RUN_ALLOW_UNAUTHENTICATED. Never on a reachable host.
# EVENTS_ALLOW_UNAUTHENTICATED=1

# ── Preloading a roster of sub-agents ─────────────────────────────────────────
# Point this at a supervisor YAML and the server starts AS a supervisor: GET /run/agents lists the
# sub-agents and POST /run can address them by name.
# CUGA_SUPERVISOR_ROSTER="events/examples/rosters/default.yaml"
#
# Export BOTH. The roster names apps, and the tool registry only serves them in FILE mode — unset,
# it runs in managed-config-db mode and serves the demo app alone, so every roster agent answers
# "the available toolset does not include…". `cuga start` no longer overwrites what you export here.
# MCP_SERVERS_FILE="src/cuga/backend/tools_env/registry/config/mcp_servers_cuga_apps.yaml"

# ── THE MASTER SWITCH ─────────────────────────────────────────────────────────
# Off by default. Nothing below has any effect without it: /run and /run/agents are not mounted, no
# roster is imported at startup, and `/automate …` stays ordinary text for the agent to answer.
#
# It exists because the individual variables get set for other reasons — GATEWAY_TOKEN is a generic
# shared secret — and eventing switching itself on as a side effect of unrelated configuration is
# not something you want from an endpoint that executes an agent.
# CUGA_EVENTS_ENABLED=true

# ── The Studio UI in a SPLIT run (CUGA :7860 + eventing :8100) ────────────────
# Both are needed for the "Events Studio" entry to appear, and the failure mode is SILENT: the UI
# swallows the error, so a missing value means the entry never renders and /studio bounces back to
# /chat with nothing on screen to say why. Leave both unset only if you are not running the
# eventing service at all.
#
# Where the SPA sends /api/events/*, /api/concierge and /invoke. Served to the browser through
# /api/ui/config. Unset means "same origin as CUGA", which serves none of those routes — the SPA
# catch-all answers with HTML and the UI reads that as "events is off".
# EVENTS_API_URL="http://localhost:8100"
#
# The eventing service only sends CORS headers for the origins listed here. Without CUGA's origin
# the browser blocks the call before it is sent, which looks identical to "the service is down".
# EVENTS_CORS_ORIGINS="http://localhost:7860"

# ── Where armed flows are stored ──────────────────────────────────────────────
# Unset = a local SQLite file, which is fine for a laptop. A deployment wants PostgreSQL: container
# storage is ephemeral, so an instance replace silently deletes every armed flow.
# EVENTS_DB="postgresql://user:pass@host:5432/dbname"

# ── Composing sub-agents in the Manage UI ─────────────────────────────────────
# The roster is imported into the agent config store at startup, so its agents are ordinary agents.
# This flag is what makes them visible and editable in Manage, and what enables creating new ones
# there; with it off, `GET /api/agents` returns only cuga-default and the roster is invisible in the
# UI (it still drives /run either way). Off by default because it also exposes create/delete-agent
# to anyone with manage access.
# DYNACONF_SUPERVISOR__REGISTRY_ENABLED=true

# Copy to .env and fill in your values
# cp .env.example .env
